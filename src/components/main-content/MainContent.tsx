import * as React from 'react';
import { Container, Text } from "native-base";

interface Props {

}

export const MainContentComponent = (props: Props) => {
    return (
        <Container>
            <Text>Main Container</Text>
        </Container>
    )
}